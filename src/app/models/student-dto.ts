export interface StudentDTO{
  id: number;
  firstName: string,
  lastName: string
}
/*Yes there are way more values in the JSON when we call it from postman, but we don't have to pull all of them, only
the ones we need*/
